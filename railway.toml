[build]
builder = "NIXPACKS"
installCommand = "pnpm install --no-frozen-lockfile"
buildCommand = "pnpm --filter forsale-web build"

[deploy]
startCommand = "cd apps/web && pnpm start"
restartPolicyType = "ON_FAILURE"
