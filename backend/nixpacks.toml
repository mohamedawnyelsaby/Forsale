# ============================================
# 🐳 NIXPACKS.TOML - BUILD CONFIGURATION
# ============================================

[phases.setup]
nixPkgs = ["nodejs-20_x", "openssl"]

[phases.install]
cmds = [
  "npm ci --include=dev",
  "npx prisma generate"
]

[phases.build]
cmds = [
  "npm run build",
  "npx prisma generate"
]

[start]
cmd = "node dist/index.js"
